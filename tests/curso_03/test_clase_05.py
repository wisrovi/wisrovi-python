"""Tests de validación para Clase 05: Embeddings y Representación Vectorial Semántica."""
import math
def test_c3_clase_05():
    v1, v2 = [1.0, 0.0], [1.0, 0.0]
    dot = sum(a * b for a, b in zip(v1, v2))
    assert dot == 1.0

